#include<conio.h>
#include<stdio.h>
void main()
{
    int l,len=10,h,hig=10,d,de;
    for(h=1;h<=hig-1;h++)
    {
        printf("L\n");
        for(d=1;d<=50000;d++)
        {
            for(de=1;de<=500;de++);
        }
    }
    for(l=1;l<=len;l++)
    {
        printf("L");
        for(d=1;d<=50000;d++)
        {
            for(de=1;de<=500;de++);
        }
    }
    getch();
}
